{
  "version": "v1",
  "hooks": [
    {
      "name": "Run tests after task",
      "trigger": "PostTaskExec",
      "description": "Runs the pytest suite after each completed task to catch regressions early.",
      "action": {
        "type": "command",
        "command": "python -m pytest -q",
        "timeout": 180
      }
    }
  ]
}