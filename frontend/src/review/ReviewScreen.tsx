/** Placeholder. Task 3 replaces this with the real review screen.
 *
 * It exists so `npm run build` has something to resolve from `main.tsx`; the
 * mount Task 1 installed only serves a `dist/` that actually built.
 */
export function ReviewScreen() {
  return <p>Review</p>
}
